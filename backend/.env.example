# Port on which the express server runs
PORT=5000

# MongoDB Local Connection String (MongoDB Compass / Local Instance)
MONGODB_URI=mongodb://127.0.0.1:27017/bloodDonorFinder

# Secret key for signing and verifying JWT tokens
JWT_SECRET=your_jwt_secret_key_here
